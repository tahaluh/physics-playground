# physics-playground

## Fast build/run loop

Use incremental builds for speed:

```bash
make run
```

That will configure the project if needed, rebuild only what changed, and run `app`.

If you want a full clean rebuild:

```bash
make rebuild
```

Other useful commands:

```bash
make build
make clean
```
